<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/>
	</parent>

	<groupId>com.protify</groupId>
	<artifactId>portfolio-backend</artifactId>
	<version>0.1.0-SNAPSHOT</version>
	<packaging>jar</packaging>
	<name>portfolio-backend</name>
	<description>Protify Portfolio Manager — single-module backend, packages owned by convention</description>

	<properties>
		<java.version>21</java.version>

		<!-- versions pinned here and nowhere else — CLAUDE.md: the POM owns all versions -->
		<mysql-connector-j.version>8.4.0</mysql-connector-j.version>
		<flyway.version>11.9.1</flyway.version>
		<springdoc-openapi.version>2.8.5</springdoc-openapi.version>
		<caffeine.version>3.1.8</caffeine.version>
		<testcontainers.version>1.20.4</testcontainers.version>
		<assertj.version>3.26.3</assertj.version>
		<mockito.version>5.14.2</mockito.version>
		<archunit.version>1.3.0</archunit.version>
		<jacoco-maven-plugin.version>0.8.12</jacoco-maven-plugin.version>
		<maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
		<maven-failsafe-plugin.version>3.5.2</maven-failsafe-plugin.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>${testcontainers.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.mockito</groupId>
				<artifactId>mockito-bom</artifactId>
				<version>${mockito.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- PortfolioApplication and the first controllers land today (D1-C1/C2/C3) — this is
		     that "add it when" moment the old comment here used to point at. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc-openapi.version}</version>
		</dependency>
		<!-- day-1-dev-B.md D1-B1: SecurityConfig lands, so the full starter (autoconfiguration +
		     filter chain) replaces the exception-types-only spring-security-core dependency below.
		     spring-security-core stays as a transitive dependency of this starter; kept explicit
		     too since GlobalExceptionHandler names it directly. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
		</dependency>

		<!-- day-2-dev-B.md D2-B2: CachingMarketDataService / CachingFxRateService. Version already
		     pinned above in <properties> in anticipation of this. -->
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
			<version>${caffeine.version}</version>
		</dependency>

		<!-- day-5-dev-C.md D5-C1: GraphQL, read-only in v1. Version managed by
		     spring-boot-starter-parent, same as every other spring-boot-starter-* here. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-graphql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-jdbc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
			<version>${flyway.version}</version>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
			<version>${flyway.version}</version>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<version>${mysql-connector-j.version}</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- day-5-dev-C.md D5-C1: GraphQlTester for GraphQlQueryTest. HttpGraphQlTester binds to
		     MockMvc via WebTestClient, which lives in spring-webflux — needed here purely as a
		     test-scope bridge class, not a second web stack; the app stays servlet-only
		     (spring.main.web-application-type is never set to reactive). -->
		<dependency>
			<groupId>org.springframework.graphql</groupId>
			<artifactId>spring-graphql-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<version>${assertj.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mysql</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- day-4-dev-A.md D4-A1 / TEST_PLAN.md §5: ArchitectureTest. ADR-0012 removed the Maven
		     module graph that made `core -> platform` a compile error, so these rules are now the
		     only automated enforcement of the layering left. -->
		<dependency>
			<groupId>com.tngtech.archunit</groupId>
			<artifactId>archunit-junit5</artifactId>
			<version>${archunit.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Inheriting spring-boot-starter-parent only *manages* this plugin's version — it does
			     not bind it to the lifecycle. Without this declaration the `repackage` goal never
			     runs and `mvn package` emits a 421KB thin jar with no Main-Class, so the image built
			     from it dies on startup with "no main manifest attribute, in /app/app.jar". That
			     breaks a `docker compose up` rebuild and every deployment, while `mvn spring-boot:run`
			     and the whole test suite keep passing — which is why it went unnoticed. -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!-- *IT classes never run under surefire/`mvn test`; only under failsafe/`mvn verify`.
			     Naming (*IT) already excludes them from surefire's default includes and includes
			     them in failsafe's — this just turns failsafe on. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven-surefire-plugin.version}</version>
				<configuration>
					<argLine>${surefireArgLine}</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>${maven-failsafe-plugin.version}</version>
				<configuration>
					<!-- Without this, the agent never attaches to the failsafe JVM and every line
					     covered only by an *IT — which is most of the repository layer — reads as
					     zero. The gate below would then be measuring the unit suite alone. -->
					<argLine>${failsafeArgLine}</argLine>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!-- day-4-dev-A.md D4-A3 / TEST_PLAN.md §3. Unit and integration coverage are
			     collected into separate exec files, merged, and then reported and gated as one
			     bundle. -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco-maven-plugin.version}</version>
				<configuration>
					<!-- TEST_PLAN.md §3's exclusion table, with its reason for each. Applies to
					     the report and the gate alike. -->
					<excludes>
						<!-- records with no logic; the mappers that build them are tested -->
						<exclude>**/dto/**</exclude>
						<exclude>**/*Response.*</exclude>
						<exclude>**/*Request.*</exclude>
						<!-- Spring wiring; proven by the context loading at all -->
						<exclude>**/config/**</exclude>
						<!-- a main method -->
						<exclude>**/PortfolioApplication.*</exclude>
						<!-- covered transitively by every repository IT against real MySQL -->
						<exclude>**/*RowMapper.*</exclude>
					</excludes>
				</configuration>
				<executions>
					<execution>
						<id>jacoco-prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<destFile>${project.build.directory}/jacoco-ut.exec</destFile>
							<propertyName>surefireArgLine</propertyName>
						</configuration>
					</execution>
					<execution>
						<id>jacoco-prepare-agent-integration</id>
						<goals>
							<goal>prepare-agent-integration</goal>
						</goals>
						<configuration>
							<destFile>${project.build.directory}/jacoco-it.exec</destFile>
							<propertyName>failsafeArgLine</propertyName>
						</configuration>
					</execution>
					<execution>
						<id>jacoco-merge</id>
						<phase>post-integration-test</phase>
						<goals>
							<goal>merge</goal>
						</goals>
						<configuration>
							<fileSets>
								<fileSet>
									<directory>${project.build.directory}</directory>
									<includes>
										<include>jacoco-ut.exec</include>
										<include>jacoco-it.exec</include>
									</includes>
								</fileSet>
							</fileSets>
							<destFile>${project.build.directory}/jacoco-merged.exec</destFile>
						</configuration>
					</execution>
					<execution>
						<id>jacoco-report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
						<configuration>
							<dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
						</configuration>
					</execution>
					<!-- The gate. TEST_PLAN.md §3: "Coverage is a floor, not a goal." If this
					     goes red the answer is a real test, never a lower number here. -->
					<execution>
						<id>jacoco-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.70</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.60</minimum>
										</limit>
									</limits>
								</rule>
								<!-- Where the money logic lives. These are pure functions with no
								     Spring, no database and no clock, so 90% is cheap here in a way
								     it would not be elsewhere. -->
								<rule>
									<element>CLASS</element>
									<includes>
										<include>com.protify.portfolio.holding.ProjectionEngine</include>
										<include>com.protify.portfolio.common.money.MoneyUtils</include>
										<include>com.protify.portfolio.valuation.ValuationService</include>
										<include>com.protify.portfolio.valuation.PerformanceService</include>
										<include>com.protify.portfolio.fx.CachingFxRateService</include>
									</includes>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.90</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
