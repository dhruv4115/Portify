# day-5-dev-C.md D5-C1: "GraphiQL enabled in the local profile only." Activate with
# --spring.profiles.active=local (or SPRING_PROFILES_ACTIVE=local) for `mvn spring-boot:run`;
# never set in the base application.properties, so it stays off everywhere else by default.
spring.graphql.graphiql.enabled=true
